{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node start.js",
    "buildCommand": "npm install",
    "numReplicas": 1,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
